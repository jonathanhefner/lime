include ':app'
::if (ANDROID_LIBRARY_PROJECTS)::::foreach (ANDROID_LIBRARY_PROJECTS)::include ':deps:::name::'
::end::::end::

::if (ANDROID_ASSET_PACKS)::::foreach (ANDROID_ASSET_PACKS)::include ':::__current__::'
::end::::end::